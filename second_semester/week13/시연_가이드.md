# Week 13 시연 가이드

## 🚀 시연 순서

### 1단계: 서버 실행 (터미널 1)

```bash
cd /Users/kyeongbin/codyssey/ia-codyssey/second_semester/week13
source venv/bin/activate
uvicorn main:app --reload
```

**확인:** 서버가 `http://127.0.0.1:8000`에서 실행되는지 확인

---

### 2단계: 요구사항 확인 (터미널 2)

```bash
cd /Users/kyeongbin/codyssey/ia-codyssey/second_semester/week13
./시연_확인.sh
```

또는 개별 확인:

```bash
# 1. contextlib 확인
grep "@contextmanager" database.py

# 2. Depends 확인
grep "Depends" domain/question/question_router.py

# 3. 스키마 확인
cat schemas.py

# 4. 스키마 사용 확인
grep "from schemas" domain/question/question_router.py
```

---

### 3단계: API 테스트

**방법 1: Swagger UI (권장)**

-   브라우저에서 http://localhost:8000/docs 접속
-   `GET /api/question/` 클릭
-   "Try it out" → "Execute"
-   응답 확인

**방법 2: curl**

```bash
curl http://localhost:8000/api/question/ | python3 -m json.tool
```

---

### 4단계: 보너스 과제 (선택)

```bash
python3 test_orm_mode.py
```

---

## 📋 핵심 확인 사항

1. ✅ `database.py`: `@contextmanager` 데코레이터
2. ✅ `question_router.py`: `Depends(get_db)` 사용
3. ✅ `schemas.py`: Pydantic 스키마 파일 존재
4. ✅ API 정상 작동: http://localhost:8000/docs

---

## ⚡ 빠른 명령어 모음

```bash
# 서버 실행
cd week13 && source venv/bin/activate && uvicorn main:app --reload

# 요구사항 확인
cd week13 && ./시연_확인.sh

# API 테스트
curl http://localhost:8000/api/question/ | python3 -m json.tool

# 보너스 테스트
cd week13 && python3 test_orm_mode.py
```
